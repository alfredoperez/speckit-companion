enabled: true
exempt: ["**/*.config.*", "**/*.test.*", "**/__tests__/**", "**/__stories__/**", "**/*.stories.*", "**/migrations/**", "**/index.ts"]
capabilities:
  - name: spec-editor
    match: ["src/features/spec-editor/**"]
    spec: src/features/spec-editor/spec-editor.spec.md
  - name: editor-ui
    match: ["webview/src/spec-editor/**"]
    spec: webview/src/spec-editor/editor-ui.spec.md
  - name: webview-shared-rendering
    match: ["webview/src/shared/**"]
    spec: capabilities/webview-shared/rendering.spec.md
  - name: webview-shared-interaction
    match: ["webview/src/shared/**"]
    spec: capabilities/webview-shared/interaction.spec.md
  - name: webview-shared-design-system
    match: ["webview/src/shared/**"]
    spec: capabilities/webview-shared/design-system.spec.md
  - name: core-spec-discovery
    match: ["src/core/specDirectoryResolver*"]
    spec: src/core/core-spec-discovery.spec.md
  - name: core-spec-context
    match: ["src/core/types/**"]
    exclude: ["src/core/types/config.ts"]
    spec: src/core/core-spec-context.spec.md
  - name: core-telemetry
    match: ["src/core/telemetry.ts", "src/core/__tests__/**"]
    spec: src/core/core-telemetry.spec.md
  - name: core-primitives
    match: ["src/core/**"]
    exclude: ["src/core/specDirectoryResolver*", "src/core/types/**", "src/core/telemetry.ts", "src/core/__tests__/**"]
    spec: src/core/core-primitives.spec.md
  - name: specs-state-record
    match: ["src/features/specs/**"]
    spec: src/features/specs/specs-state-record.spec.md
  - name: specs-lifecycle
    match: ["src/features/specs/**"]
    spec: src/features/specs/specs-lifecycle.spec.md
  - name: specs-commands
    match: ["src/features/specs/**"]
    spec: src/features/specs/specs-commands.spec.md
  - name: specs-pipeline-build
    match: ["src/features/specs/**"]
    spec: src/features/specs/specs-pipeline-build.spec.md
  - name: specs-living-view
    match: ["src/features/specs/**"]
    spec: src/features/specs/specs-living-view.spec.md
  - name: specs-living-model
    match: ["src/features/specs/**"]
    spec: src/features/specs/specs-living-model.spec.md
  - name: steering-tree
    match: ["src/features/steering/**"]
    spec: src/features/steering/steering-tree.spec.md
  - name: steering-companion
    match: ["src/features/steering/**"]
    spec: src/features/steering/steering-companion.spec.md
  - name: steering-documents
    match: ["src/features/steering/**"]
    spec: src/features/steering/steering-documents.spec.md
  - name: spec-viewer-panel
    match: ["src/features/spec-viewer/**"]
    spec: src/features/spec-viewer/spec-viewer-panel.spec.md
  - name: spec-viewer-state
    match: ["src/features/spec-viewer/**"]
    spec: src/features/spec-viewer/spec-viewer-state.spec.md
  - name: spec-viewer-actions
    match: ["src/features/spec-viewer/**"]
    spec: src/features/spec-viewer/spec-viewer-actions.spec.md
  - name: spec-viewer-living
    match: ["src/features/spec-viewer/**"]
    spec: src/features/spec-viewer/spec-viewer-living.spec.md
  - name: commands-assembly
    match: ["speckit-extension/commands/**", "speckit-extension/nodes/**", "speckit-extension/presets/**"]
    spec: capabilities/companion-commands/assembly.spec.md
  - name: commands-nodes
    match: ["speckit-extension/commands/**", "speckit-extension/nodes/**", "speckit-extension/presets/**"]
    spec: capabilities/companion-commands/nodes.spec.md
  - name: commands-pipeline
    match: ["speckit-extension/commands/**", "speckit-extension/nodes/**", "speckit-extension/presets/**"]
    spec: capabilities/companion-commands/pipeline.spec.md
  - name: commands-capture
    match: ["speckit-extension/commands/**", "speckit-extension/nodes/**", "speckit-extension/presets/**"]
    spec: capabilities/companion-commands/capture.spec.md
  - name: commands-completion
    match: ["speckit-extension/commands/**", "speckit-extension/nodes/**", "speckit-extension/presets/**"]
    spec: capabilities/companion-commands/completion.spec.md
  - name: commands-living-markers
    match: ["speckit-extension/scripts/living_validate.py", "speckit-extension/scripts/living_spec_fold.py"]
    spec: capabilities/companion-commands/living-markers.spec.md
  - name: commands-living
    match: ["speckit-extension/commands/**", "speckit-extension/nodes/**", "speckit-extension/presets/**"]
    spec: capabilities/companion-commands/living-commands.spec.md
  - name: commands-living-load
    match: ["speckit-extension/commands/**", "speckit-extension/nodes/**", "speckit-extension/presets/**"]
    spec: capabilities/companion-commands/living-load.spec.md
  - name: workflows-catalog
    match: ["src/features/workflows/**"]
    spec: capabilities/workflows/workflows-catalog.spec.md
  - name: workflows-selection
    match: ["src/features/workflows/**"]
    spec: capabilities/workflows/workflows-selection.spec.md
  - name: workflows-persistence
    match: ["src/features/workflows/**"]
    spec: capabilities/workflows/workflows-persistence.spec.md
  - name: workflows-pipeline
    match: ["src/features/workflows/**", "speckit-extension/workflows/**"]
    spec: capabilities/workflows/workflows-pipeline.spec.md
  - name: ai-providers-dispatch
    match: ["src/ai-providers/**"]
    spec: src/ai-providers/ai-providers-dispatch.spec.md
  - name: ai-providers-prompt
    match: ["src/ai-providers/**"]
    spec: src/ai-providers/ai-providers-prompt.spec.md
  - name: ai-providers-config
    match: ["src/ai-providers/**"]
    spec: src/ai-providers/ai-providers-config.spec.md
  - name: asset-discovery
    match: ["src/features/agents/**", "src/features/skills/**"]
    spec: capabilities/extension-services/asset-discovery.spec.md
  - name: extension-settings
    match: ["src/features/settings/**", "src/features/permission/**"]
    spec: capabilities/extension-services/extension-settings.spec.md
  - name: speckit-cli-detection
    match: ["src/speckit/detector.ts", "src/speckit/cliCommands.ts", "src/speckit/specKitAgent.ts", "src/speckit/index.ts"]
    spec: src/speckit/speckit-cli-detection.spec.md
  - name: speckit-cli-companion-extension
    match: ["src/speckit/specKitExtensionInstall.ts", "src/speckit/specKitExtensionInstallCommands.ts", "src/speckit/companionVersionGap.ts", "src/speckit/companionUpdateNudge.ts"]
    spec: src/speckit/speckit-cli-companion-extension.spec.md
  - name: speckit-cli-notifications
    match: ["src/speckit/updateChecker.ts", "src/speckit/utilityCommands.ts", "src/speckit/taskProgressService.ts"]
    spec: src/speckit/speckit-cli-notifications.spec.md
  - name: capture-runtime-context
    match: ["speckit-extension/scripts/spec_context.py", "speckit-extension/scripts/write-context.py", "speckit-extension/scripts/derive-from-files.py"]
    spec: speckit-extension/scripts/capture-runtime-context.spec.md
  - name: capture-runtime-capture
    match: ["speckit-extension/scripts/capture.py", "speckit-extension/scripts/run_trace.py", "speckit-extension/scripts/status-context.py"]
    spec: speckit-extension/scripts/capture-runtime-capture.spec.md
  - name: capture-runtime-tasks
    match: ["speckit-extension/scripts/task_sync.py"]
    spec: speckit-extension/scripts/capture-runtime-tasks.spec.md
  - name: capture-runtime-living-fold
    match: ["speckit-extension/scripts/living_spec_fold.py", "speckit-extension/scripts/spec_deltas.py", "speckit-extension/scripts/living_validate.py"]
    spec: speckit-extension/scripts/capture-runtime-living-fold.spec.md
  - name: capture-runtime-living-resolve
    match: ["speckit-extension/scripts/resolve-spec-paths.py", "speckit-extension/scripts/record-living-specs.py", "speckit-extension/scripts/register-capability.py", "speckit-extension/scripts/relocate-capability.py"]
    spec: speckit-extension/scripts/capture-runtime-living-resolve.spec.md
  - name: capture-runtime-drift
    match: ["speckit-extension/scripts/drift.py", "speckit-extension/scripts/doctor_drift.py"]
    spec: speckit-extension/scripts/capture-runtime-drift.spec.md
  - name: capture-runtime-reports
    match: ["speckit-extension/scripts/drift.py", "speckit-extension/scripts/check-coverage.py", "speckit-extension/scripts/doctor.py", "speckit-extension/scripts/doctor_checks.py", "speckit-extension/scripts/doctor_bleed.py", "speckit-extension/scripts/doctor_chat.py", "speckit-extension/scripts/doctor_drift.py"]
    spec: speckit-extension/scripts/capture-runtime-reports.spec.md
  - name: capture-runtime-build
    match: ["speckit-extension/scripts/build-pipeline.py", "speckit-extension/scripts/assemble-nodes.py", "speckit-extension/scripts/build-commands.py", "speckit-extension/scripts/hook_render.py", "speckit-extension/scripts/template_render.py", "speckit-extension/scripts/decision_routes.py", "speckit-extension/scripts/manifest.py", "speckit-extension/scripts/emission_sync.py", "speckit-extension/scripts/package-manifest.py", "speckit-extension/scripts/_command_parts.py", "speckit-extension/scripts/capture-golden.py", "speckit-extension/scripts/check-shape-parity.py", "speckit-extension/scripts/check-command-emissions.py", "speckit-extension/scripts/instruction-budget.py", "speckit-extension/scripts/instruction-budget.json"]
    spec: speckit-extension/scripts/capture-runtime-build.spec.md
  - name: capture-runtime-config
    match: ["speckit-extension/scripts/companion_config.py", "speckit-extension/scripts/config_write.py", "speckit-extension/scripts/config_repair.py", "speckit-extension/scripts/pipeline-graph.py"]
    spec: speckit-extension/scripts/capture-runtime-config.spec.md
  - name: viewer-ui-state
    match: ["webview/src/spec-viewer/**"]
    spec: webview/src/spec-viewer/viewer-ui-state.spec.md
  - name: viewer-ui-navigation
    match: ["webview/src/spec-viewer/**"]
    spec: webview/src/spec-viewer/viewer-ui-navigation.spec.md
  - name: viewer-ui-document
    match: ["webview/src/spec-viewer/**"]
    spec: webview/src/spec-viewer/viewer-ui-document.spec.md
  - name: viewer-ui-comments
    match: ["webview/src/spec-viewer/**"]
    spec: webview/src/spec-viewer/viewer-ui-comments.spec.md
  - name: viewer-ui-overview
    match: ["webview/src/spec-viewer/**"]
    spec: webview/src/spec-viewer/viewer-ui-overview.spec.md
  - name: viewer-ui-chrome
    match: ["webview/src/spec-viewer/**"]
    spec: webview/src/spec-viewer/viewer-ui-chrome.spec.md

# House rules, written once here instead of retyped into chat on every run.
# `spec` reaches the specify step, `plan` reaches the plan step, and neither
# sees the other's.
rules:
  spec:
    - "Write every scenario as WHEN/THEN with exactly one observable outcome"
    - "Reserve backticks for real identifiers a reader would copy, never ordinary nouns"
  plan:
    - "Name the capability each decision belongs to, so the fold knows where it goes"
    - "Prefer extending the file that already owns a concern over adding a new one"

# Requirements dropped in the 2026-09-07 re-adoption, each because a test
# already enforces it. Kept here so the decision is greppable.
#   One message dispatcher serves both ends of the webview protocol: dropped because the TypeScript compiler already enforces both halves. The dispatcher'
#   A provider's label and its mark resolve from one decision — enforced by src/features/steering/__tests__/providerIcon.test.ts (unknown host and unknown
#   A file the view creates lands where the view watches and reads — enforced by src/features/steering/__tests__/steeringManager.test.ts (global steering 
#   A done spec offers only its finish actions, never the forward advance — already pinned by a test: src/features/spec-viewer/__tests__/footerMatrix.fixt
#   A CLI provider is a terminal target; an editor-chat provider is not — every scenario (three editor providers false, CLIs true, unknown defaults to ter
#   The preamble's schema block is generated from the canonical contract, never retyped — its one scenario (every author/step/status listed from the canon
#   Command names are rewritten to whatever the target actually registered — dot/dash per target, user-setting override, verb-only rewrite, non-SpecKit co
#   A value's meaning survives a type change — enforced by src/core/settingsMigration.test.ts (`coerceLegacyBoolean` maps beta/on→true, off→false, boolean
#   Migrations rewrite only known legacy values, scope by scope, and are idempotent — enforced by src/core/settingsMigration.test.ts (`preserves scope`, `
#   Retired settings are removed from settings but tolerated if present — removal half enforced by src/core/settingsMigration.test.ts `removeRetiredSettin
#   Reconciling the companion's command family is add-only and idempotent — enforced by src/features/settings/companionPresetReconciler.test.ts (`is a no-
#   An install-state gate keys on the signal that actually implies the capability — enforced scenario-for-scenario by companionPresetReconciler.test.ts `i
#   A recorded step completion settles the step even when status lags — both scenarios are enforced one-to-one by webview/src/spec-viewer/__tests__/stepIn
#   A teardown must unmount what it marked, not re-find it — enforced by webview/src/spec-viewer/editor/editorHost.test.ts ('leaves no editing class behin

