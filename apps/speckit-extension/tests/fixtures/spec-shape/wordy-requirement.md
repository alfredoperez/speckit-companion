# Wordy requirement

## Requirements

### Each spec gets one row

The panel SHALL show one row per spec it finds. The row reads as the folder name made readable, because people scan a list by name and not by number. The row reads as the folder name made readable, because people scan a list by name and not by number. The row reads as the folder name made readable, because people scan a list by name and not by number. The row reads as the folder name made readable, because people scan a list by name and not by number. The row reads as the folder name made readable, because people scan a list by name and not by number. The row reads as the folder name made readable, because people scan a list by name and not by number.

#### Scenario: a folder holds a spec
- **WHEN** the panel refreshes
- **THEN** one row appears for it
