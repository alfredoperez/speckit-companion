## Purpose

Fold-back appends to the end of the file, so requirements sit past the uncovered section.

## Requirements

### Written when the capability was adopted

It MUST do the thing.

#### Scenario: the ordinary case
- **WHEN** asked
- **THEN** it does it

## Uncovered

- `src/skimmed.ts`

### Appended by a later fold, and still a requirement

It MUST do the other thing, and it has no scenario, which must still be reported.
