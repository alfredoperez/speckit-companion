## Purpose

A well-formed spec that produces no findings at all.

## Requirements

### The thing behaves
<!-- touches: speckit-extension/scripts/**  -->

It does the thing, and it does it once.

#### Scenario: the ordinary case
- **WHEN** the thing is asked for
- **THEN** it is produced

### The other thing behaves

It does the other thing.

#### Scenario: the ordinary case again
- **WHEN** the other thing is asked for
- **THEN** it is produced
