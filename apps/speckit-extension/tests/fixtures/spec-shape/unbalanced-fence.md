## Purpose

A fence is opened and never closed, so nothing below it is visible.

## Requirements

### A rule before the fence

Prose.

#### Scenario: the ordinary case
- **WHEN** asked
- **THEN** it does it

```sh
echo this fence never closes

### A rule nothing can see

Invisible to every reader.
