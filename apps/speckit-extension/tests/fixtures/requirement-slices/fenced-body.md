## Purpose

A requirement whose prose carries a code example.

## Requirements

### The shape is declared
<!-- touches: src/shape.ts -->

The shape is:

```json
{"heading": "…", "touches": []}
```

That is the contract.

### Nothing fenced here

Plain prose only.
