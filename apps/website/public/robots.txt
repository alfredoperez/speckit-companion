# Everything here is public marketing and documentation, so nothing is
# disallowed. The sitemap is generated by @astrojs/sitemap at build time and
# only exists because `site` is set in astro.config.mjs — if that is ever
# unset again this line points at a 404.
User-agent: *
Allow: /

Sitemap: https://speckit-companion.dev/sitemap-index.xml
