# Plan: One implement-complete marker

> Fast-tracked (small change). The implementation approach lives inline in the spec — see [`./spec.md#approach`](./spec.md#approach). The task checklist is in [`./tasks.md`](./tasks.md).
