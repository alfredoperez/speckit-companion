# Plan: A hook anchored to a name that means two things is drawn once

> Small change, planned inline. The approach lives in [`./spec.md#approach`](./spec.md#approach); the task checklist is in [`./tasks.md`](./tasks.md).
