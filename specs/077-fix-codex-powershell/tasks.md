# Tasks: Fix Codex Provider PowerShell Compatibility

**Plan**: [plan.md](./plan.md) | **Date**: 2026-04-24

## Format

- `[P]` marks tasks that can run in parallel with adjacent `[P]` tasks.
- Consecutive `[P]` tasks form a **parallel group** — `/sdd:implement` spawns them as concurrent subagents.
- Tasks without `[P]` are **gates**: they start only after all prior tasks complete.
- Two tasks that touch the same file are never both `[P]`.

---

## Phase 1: Core Implementation

- [x] **T001** [P] Add `readInitOptions` helper + tests — `src/ai-providers/initOptions.ts`, `src/ai-providers/__tests__/initOptions.test.ts` | R005, R006, NFR001, NFR002
  - **Do**: Create `src/ai-providers/initOptions.ts` exporting `readInitOptions(outputChannel?: vscode.OutputChannel): { script: 'sh' | 'ps' }` and `resetInitOptionsCache(): void`. The reader resolves the first workspace folder, reads `.specify/init-options.json`, and coerces the `script` field to `'ps'` only when it equals `"ps"`; anything else (missing file, unreadable, malformed JSON, unknown value) normalizes to `'sh'`. Cache the result per workspace-folder path in a module-level `Map<string, InitOptions>` so re-entry is a no-op; `resetInitOptionsCache` clears it for tests. On read/parse failure, call `outputChannel?.appendLine(...)` with a single line that includes the failure reason and the detected workspace path — NEVER include file contents or prompts (NFR002). Never throw. Also create `src/ai-providers/__tests__/initOptions.test.ts` with BDD-style `describe`/`it` covering: (a) missing `init-options.json` returns `{ script: 'sh' }` and does not throw; (b) valid `{"script":"ps"}` returns `{ script: 'ps' }`; (c) invalid JSON logs one warning to a mock output channel and returns `{ script: 'sh' }`; (d) unknown script value (e.g. `"bash"`) normalizes to `'sh'`; (e) second call with the same workspace uses the cache (assert `fs.readFileSync` or `fs.promises.readFile` was invoked exactly once across two calls). Call `resetInitOptionsCache()` in a `beforeEach`.
  - **Verify**: `npm run compile` passes; `npm test -- initOptions` passes.
  - **Leverage**: `src/core/specDirectoryResolver.ts` (pattern for resolving workspace-folder paths + safe fs reads); `tests/__mocks__/vscode.ts` (mock `OutputChannel` / `workspace.workspaceFolders`).

- [x] **T002** [P] Add `buildCodexExecCommand` helper + tests — `src/ai-providers/codexCommandBuilder.ts`, `src/ai-providers/__tests__/codexCommandBuilder.test.ts` | R001, R003, R004, R007, NFR003
  - **Do**: Create `src/ai-providers/codexCommandBuilder.ts` exporting a pure function `buildCodexExecCommand(opts: { script: 'sh' | 'ps'; promptFilePath: string; permissionFlag?: string }): string`. For `script === 'ps'` return `` `Get-Content "${promptFilePath}" -Raw | codex exec ${permissionFlag ? permissionFlag + ' ' : ''}-` ``. For `script === 'sh'` (and any fallback) return `` `cat "${promptFilePath}" | codex exec ${permissionFlag ? permissionFlag + ' ' : ''}-` ``. No `<` redirection anywhere (R001). The function must be side-effect free — no fs, no vscode imports — so it's trivially unit-testable. Also create `src/ai-providers/__tests__/codexCommandBuilder.test.ts` with BDD tests: (a) `sh` shape emits `cat "<file>" | codex exec -` and contains no `<` character besides the one inside quotes (assert with a regex `/(^|\s)</`); (b) `ps` shape emits `Get-Content "<file>" -Raw | codex exec -`; (c) permission flag is inserted _before_ `-` when non-empty, omitted when absent/empty; (d) both shapes use the exact `promptFilePath` verbatim (no escaping — the helper trusts the caller to pass a safe path because the file name is generated by `createTempFile`).
  - **Verify**: `npm run compile` passes; `npm test -- codexCommandBuilder` passes; both shell shapes covered (NFR003).
  - **Leverage**: `src/ai-providers/claudeCodeProvider.ts` (current `"$(cat "…")"` pattern for reference on how Codex provider currently assembles commands) — copy the structural idea, not the exact syntax.

- [x] **T003** Rewrite Codex provider execute paths to use helpers — `src/ai-providers/codexCliProvider.ts` _(depends on T001, T002)_ | R001, R002, R003, R004, R006, R007
  - **Do**: In `src/ai-providers/codexCliProvider.ts`: (1) delete the `escapeForSed` function and any bash-only `sed | codex exec -` / `codex exec - < "$file"` command assembly across all three entry points (`executeInTerminal`, `executeHeadless`, `executeSlashCommand`). (2) For each entry point, resolve the prompt text in Node: when the first line of the incoming prompt matches `/speckit.<skill>` and the matching file exists under `.codex/prompts/`, read the file, substitute every `$ARGUMENTS` occurrence with the user's arguments using plain JS string replace (no shell escaping needed — the content is written to a temp file, not passed via shell). For the custom/fallback path, use the incoming prompt (or existing `"Run the following SpecKit command: …"` wrapper) unchanged. (3) Call the existing `createPromptFile(resolvedPrompt, prefix)` to obtain a temp file path. (4) Call `readInitOptions(this.outputChannel)` (imported from `./initOptions`) to detect shell. (5) Build the terminal command via `buildCodexExecCommand({ script, promptFilePath, permissionFlag })` (imported from `./codexCommandBuilder`). (6) Preserve the current `Timing.tempFileCleanupDelay` cleanup scheduling. (7) Log exactly one line to `this.outputChannel` per dispatch: `"[codex] script=<sh|ps> (init-options)"` — never the prompt content (NFR002). Make sure `executeHeadless` and `executeSlashCommand` follow the same pattern so no path keeps the old `<` redirection. Remove any now-unused imports.
  - **Verify**: `npm run compile` passes; `npm test` passes (existing Codex provider tests still green; if an existing test hard-codes the old `sed | codex exec -` or `codex exec - <` string, update that test expectation to the new `cat "<file>" | codex exec -` shape — do not introduce new `[P]` work for this, fix it inline). Manually inspect the three entry points to confirm no `<` character appears in any command string built for the terminal.
  - **Leverage**: `src/ai-providers/codexCliProvider.ts` current structure (keep entry-point signatures, `createPromptFile`, `waitForShellReady`, and `executeCommandInHiddenTerminal` helpers unchanged); `src/ai-providers/claudeCodeProvider.ts` (reference for how a provider handles per-dispatch prompt resolution cleanly).
