# Tasks: Picker Companion Label

- [x] **T001** [P] Relabel the picker choice: `displayName: 'Turbo'` → `'SpecKit Companion'` and reword the adjacent `description` to frame it as pinning the leaner SpecKit Companion (turbo) `/speckit.companion.*` pipeline regardless of the project default + `src/features/spec-editor/specEditorProvider.ts`
- [x] **T002** [P] Reword the `speckit.companion.turboWorkflowPicker` `markdownDescription` so the quoted choice name reads "SpecKit Companion" (drop "(Turbo)" from the choice name only; key unchanged) + `package.json`
- [x] **T003** [P] Update the picker-choice label wording in the per-spec turbo section + `README.md`
- [x] **T004** [P] Update the picker-choice label wording (leave the ceremony-axis Turbo/Standard description intact) + `docs/template-profiles.md`
- [x] **T005** Verify the Create-Spec picker screenshot; re-shoot in place only if it shows "Turbo" (overwrite, never rename/delete) + `docs/screenshots/create-spec.png`
