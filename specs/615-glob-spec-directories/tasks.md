# Tasks: Glob spec directories list the specs inside them

- [x] **T001** Add a shared own-content test and apply container semantics in the glob deep-file pass and fallback + src/core/specDirectoryResolver.ts
- [x] **T002** Attribute files through a contentless glob match to the subfolder one level below + src/core/specDirectoryResolver.ts
- [x] **T003** [P] Cover the container layout, an empty container, and attribution through a container + src/core/specDirectoryResolver.test.ts
- [x] **T004** [P] Update the setting description, configuration doc and changelog + package.json, docs/configuration.md, CHANGELOG.md
