# Plan — Fast-path lean files (#237)

> **Fast-tracked (simple mode).** This change was small enough to fast-path. The plan content lives inline in the spec — see [**Approach** in `spec.md`](./spec.md#approach) for the files to touch and dependencies. The task checklist is in [`tasks.md`](./tasks.md).
