# Tasks — Reorder Beta Features Settings

- [x] **T001** Add `order` (1–6) to each Beta Features setting in funnel order (installPrompt→templateProfile→turboWorkflowPicker→complexityFastPath→resumeBeta→viewer.activityPanel) + `package.json`
- [x] **T002** Convert the five dependency-bearing settings to `markdownDescription` and append the consistent "Requires the [companion spec-kit extension](…)" note with install link; leave installPrompt without a note + `package.json`
- [x] **T003** Verify the `#installing-the-spec-kit-extension` README anchor exists and adjust the link target if the heading differs + `README.md`
- [x] **T004** Update the README "Configuration" → Beta Features subsection to reflect the new order and dependency notes + `README.md`
