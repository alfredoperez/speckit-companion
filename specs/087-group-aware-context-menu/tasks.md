# Tasks: Group-aware Context Menu

**Plan**: [plan.md](./plan.md) | **Date**: 2026-04-27

## Format

- `[P]` marks tasks that can run in parallel with adjacent `[P]` tasks.
- Consecutive `[P]` tasks form a **parallel group** — `/sdd:implement` spawns them as concurrent subagents.
- Tasks without `[P]` are **gates**: they start only after all prior tasks complete.
- Two tasks that touch the same file are never both `[P]`.

---

## Phase 1: Core Implementation

- [x] **T001** Encode lifecycle group into spec tree-item `viewItem` — `src/features/specs/specExplorerProvider.ts` | R001, NFR001
  - **Do**: Add two top-level helpers in `specExplorerProvider.ts` (export both): `lifecycleContextValue(specContext: FeatureWorkflowContext | undefined): 'spec-active' | 'spec-tasks-done' | 'spec-completed' | 'spec-archived'` — switches on `specContext?.status` against `SpecStatuses.{TASKS_DONE,COMPLETED,ARCHIVED}` and falls back to `'spec-active'` for `ACTIVE`/missing/unknown — and `isSpecLifecycleItem(cv: string | undefined): boolean` — returns true iff `cv` is one of those four. In `getChildren` (group-expansion branch around line 215), pass `lifecycleContextValue(specContext)` into the `SpecItem` constructor instead of the literal `'spec'`. Update the two strict `element.contextValue === 'spec'` checks in `getChildren` (lines 204, 244) and the `contextValue === 'spec'` branch inside the `SpecItem` constructor (line 612) to call `isSpecLifecycleItem(...)` instead. Do **not** add any extra `.spec-context.json` reads — the `specContext` already loaded in the group branch is the only source.
  - **Verify**: `npm run compile` passes. `grep -n "=== 'spec'" src/features/specs/specExplorerProvider.ts` returns no remaining strict-equality matches against the literal `'spec'`. In the Extension Development Host, expanding the Active/Completed/Archived groups still renders specs with the right icon/tooltip per group.
  - **Leverage**: The existing `specContext?.status === SpecStatuses.COMPLETED` branch at line 615 — same status-resolution shape, just lifted into the helper.

- [x] **T002** [P] Switch context-menu `when` clauses to per-`viewItem` matches *(depends on T001)* — `package.json` | R002, R003, R004, R005, R006
  - **Do**: In `contributes.menus."view/item/context"`, replace the three lifecycle entries (`speckit.markCompleted` line 489, `speckit.archive` line 494, `speckit.reactivate` line 499). New `when` values: `markCompleted` → `view == speckit.views.explorer && (viewItem == spec-active || viewItem == spec-tasks-done)`; `archive` → `view == speckit.views.explorer && viewItem =~ /^spec-(active|tasks-done|completed)$/`; `reactivate` → `view == speckit.views.explorer && (viewItem == spec-completed || viewItem == spec-archived)`. Update the always-visible spec entries (`speckit.delete` line 484, `speckit.specs.reveal` line 504, `speckit.specs.revealInExplorer` line 509) to `view == speckit.views.explorer && viewItem =~ /^spec-(active|tasks-done|completed|archived)$/`. Do not touch the `spec-document-*` or `spec-related-doc` entries.
  - **Verify**: `node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"` passes. `grep -n "speckit.specs.selection.allActive\\|allCompleted\\|allArchived" package.json` returns zero matches. In the Extension Development Host, right-clicking each lifecycle group's spec shows the menu items required by spec scenarios.

- [x] **T003** [P] Filter no-op targets and accept lifecycle `viewItem`s in bulk handlers *(depends on T001)* — `src/features/specs/specCommands.ts` | R007
  - **Do**: Import `isSpecLifecycleItem` from `./specExplorerProvider`. In `resolveTargets` (line 55), replace the strict `=== 'spec'` filter on line 59 with `isSpecLifecycleItem((s as any)?.contextValue)`. In `runBulkStatusChange` (line 255), accept a new `skipIf: (status: string | undefined) => boolean` parameter. Inside the function, after `resolveTargets`/`updateSelectionContextKeys`, read each target's status via `readSpecContextSync(specDirFor(t, workspaceFolder.uri.fsPath))?.status` (import `readSpecContextSync` from `./specContextManager`) and drop any target where `skipIf(status)` returns `true`. If the filtered list is empty, return early without calling `apply`, refreshing the tree, or showing the success toast. Update each caller: `markCompleted` passes `s => s === SpecStatuses.COMPLETED`; `archive` passes `s => s === SpecStatuses.ARCHIVED`; `reactivate` passes `s => s !== SpecStatuses.COMPLETED && s !== SpecStatuses.ARCHIVED` (i.e. skip already-active specs, including `tasks-done`). Import `SpecStatuses` from `../../core/constants` (already imported elsewhere in the file).
  - **Verify**: `npm run compile` passes. Existing `bulk status command handlers` describe block (line 159 in `specCommands.test.ts`) still passes.
  - **Leverage**: Existing `readSpecContextSync` usage in `selectionContextKeys.ts:14` — same `path.join(ws, item.specPath)` resolution shape (note: here we use `specDirFor` which already joins `ws` with `specPath`).

- [x] **T004** [P] Drop deprecated selection context-key writes *(depends on T001)* — `src/features/specs/selectionContextKeys.ts` | R008
  - **Do**: Remove the three `vscode.commands.executeCommand('setContext', ...)` calls for `speckit.specs.selection.allActive`, `speckit.specs.selection.allCompleted`, and `speckit.specs.selection.allArchived` (lines 33–35). Remove the now-unused `allActive`, `allCompleted`, `allArchived` locals and their `isCompleted`/`isArchived`/`isActive` predicates (lines 23–29). Keep `count` and `mixed` writes; recompute `mixed` inline (`count > 1` is sufficient given menu visibility no longer depends on the per-status booleans — but if any UI still expects "mixed" to mean ≠ all-same, keep the original computation by recomputing `allActive/allCompleted/allArchived` as locals scoped to the `mixed` line only). Add a one-line header comment: `// Menu visibility is driven by per-item viewItem (see SpecItem.contextValue); this module now writes only count/mixed for any UI that still depends on them.`
  - **Verify**: `npm run compile` passes. `grep -rn "speckit.specs.selection.allActive\\|allCompleted\\|allArchived" src/ package.json` returns zero matches.

- [x] **T005** Update tests for lifecycle `viewItem`s and no-op filtering *(depends on T002, T003, T004)* — `src/features/specs/specCommands.test.ts` | R001, R007
  - **Do**: Update the `makeItem` factory (line 170) signature to `(name: string, contextValue: string = 'spec-active') => ({ label: name, specPath: \`specs/\${name}\`, contextValue })`. In the existing `bulk status command handlers` describe block, change all default `makeItem(...)` calls to the appropriate lifecycle viewItem (e.g. `makeItem('foo', 'spec-active')` for items used with `markCompleted`/`archive`, `makeItem('foo', 'spec-completed')` for items used with `reactivate`). Add three new `it` cases: (a) `markCompleted` invoked on a single `spec-completed` item is a no-op (mock `readSpecContextSync` to return `{ status: 'completed' }`; assert `setStatus` is never called and the success notification is not shown); (b) `reactivate` invoked on a mixed `[spec-active, spec-completed]` selection calls the lifecycle apply only for the completed target; (c) `resolveTargets` accepts items whose contextValue is any of `spec-active`/`spec-tasks-done`/`spec-completed`/`spec-archived` and rejects `spec-document-spec` / `spec-related-doc`. Add the necessary `jest.mock('./specContextManager', ...)` (or extend the existing mock map) so `readSpecContextSync` is controllable per case.
  - **Verify**: `npm test -- specCommands.test.ts` passes. All three new cases run.
  - **Leverage**: Existing `bulk status command handlers` describe block (line 159) — same Jest mock setup pattern; just add the `readSpecContextSync` mock and reuse its structure.
