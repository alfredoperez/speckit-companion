# Tasks: Clarify Setup Components

**Plan**: [plan.md](./plan.md)

> Format reference: `[P]` markers and parallel groups — see `skills/tasks/SKILL.md` § Phase rules.

## Phase 1: Core Implementation

- [x] **T001** Add `## Setup & Components` section with Required/Optional table — `README.md` | R001, R006
  - **Do**: Insert a new `## Setup & Components` section immediately after the `## Getting Started` section. Lead with a compact table listing each component, its Required/Optional status, and a one-line role: the **extension** (Required — viewer, review comments, dispatch); the **spec-kit CLI / `specify`** (Optional — only to run `/speckit.*` phase commands); the **`.specify/` scaffolding** (Optional — generated by `specify init`, only for spec-kit phase commands); the **`companion` hook in `.specify/extensions.yml`** (Optional — wires git steps into spec-kit runs).
  - **Verify**: `## Setup & Components` renders right after Getting Started; table has 4 rows each labelled Required or Optional.

- [x] **T002** Write dispatch-vs-terminal + ownership prose and link the deep dive *(depends on T001)* — `README.md` | R002, R003, R004, R005
  - **Do**: Under the table, add three short paragraphs (single logical line each, no hard wraps): (1) the extension's only runtime coupling to a provider is dispatching command text — the viewer/review features work without the spec-kit CLI and it never reads CLI responses back; (2) `.claude/specs/` + `.spec-context.json` are the single source of truth, so a step driven from the extension or from a terminal spec-kit run are interchangeable, not competing — no separate extension-owned state; (3) reference the existing **Supported AI Providers** section for the two dispatch styles (terminal CLI vs. IDE Chat / Claude in VS Code) without restating the matrix, and add an inbound link to `docs/how-it-works.md` for the architecture deep dive.
  - **Verify**: Section names the extension as the only hard requirement, states shared-files = no ownership conflict, links to both the providers section and `docs/how-it-works.md`.

- [x] **T003** Consistency + markdown lint pass *(depends on T002)* — `README.md`
  - **Do**: Cross-check the new prose against the existing **IDE Chat** and **Claude in VS Code** notes (one-way dispatch, spec-kit-must-be-initialized) so nothing contradicts; confirm no hard-wrapped paragraphs were introduced (each prose paragraph is one logical line).
  - **Verify**: `npm test` passes (docs-consistency test green); no wording conflicts with the provider notes.
