# Tasks — Untrack the Companion config mirror

Dependency-ordered checklist, grouped by execution layer. Traceability is to files and functional requirements (`FR-…`). No source code changes are expected — the existing reconciler already creates the mirror on demand.

## Setup

- [x] T001 Confirm `.specify/companion.yml` is currently tracked: run `git ls-files --error-unmatch .specify/companion.yml` from the repo root and note the result (establishes the FR-001 starting state).

## Foundational (blocking)

- [x] T002 Untrack the mirror without deleting the working copy: `git rm --cached .specify/companion.yml` at repo root. Verify the file still exists on disk and `git status` shows it staged for deletion. Satisfies **FR-001**; parent of **FR-003** and **FR-006**.

## Core work

- [x] T003 Add `.specify/companion.yml` to `.gitignore` near the existing `.specify/presets/` / `.specify/extensions/companion/` block (~line 44–47), with a one-line comment identifying it as a machine-local mirror of the `speckit.companion.templateProfile` setting, written and regenerated by the extension. Match the wording style of the adjacent generated/local-only entries. Satisfies **FR-002**.

## Integration (verify the on-demand path, no edits)

- [x] T004 [P] Confirm (do not edit) `src/features/settings/companionPresetReconciler.ts`: `writeTemplateProfile()` does `fs.mkdirSync(dir, {recursive:true})` then writes, and `readTemplateProfile()` returns `undefined` when the file is absent. Satisfies **FR-004** with zero new code.
- [x] T005 [P] Confirm (do not edit) `src/extension.ts` (~line 251): activation mirrors the project default into the file, regenerating it on a fresh clone. Supports **FR-004**.
- [x] T006 [P] Confirm (do not edit) `src/features/specs/profileDispatch.ts`: it only reads the mirrored value; the standard/turbo routing and per-spec pinning path is unchanged. Satisfies **FR-005**.

## Documentation (FR-007)

- [x] T007 [P] Update `README.md` "Template Profiles" section to describe `.specify/companion.yml` as a machine-local, gitignored mirror of the profile setting.
- [x] T008 [P] Update `docs/template-profiles.md` (profile/selection-model reference) to describe the mirror file as machine-local/gitignored.
- [x] T009 [P] Update `docs/capture-and-timing.md` wherever the mirror is described as an install/config path, marking it machine-local/gitignored.
- [x] T010 [P] Update `speckit-extension/docs/install.md` install-path mention to describe the mirror as machine-local/gitignored.

## Polish / Validation (Success Criteria)

- [x] T011 Run `git grep -l "companion.yml" -- README.md docs/ speckit-extension/docs/` and confirm every user-doc hit now describes the file as machine-local/gitignored (the `src/` code-comment matches stay as-is). Validates **SC-005** / **FR-007**.
- [x] T012 Verify **SC-001**/**SC-002**/**SC-004**: change `speckit.companion.templateProfile`, reload the editor, and confirm `git status` reports zero modified tracked files attributable to the mirror; then delete `.specify/companion.yml`, reload, and confirm activation recreates it with no error surfaced.
- [x] T013 Verify **SC-003**: select both standard and turbo as the project default and pin each per-spec, confirming profile routing behaves exactly as before (no regression).

## Dependencies

- **T001 → T002**: confirm tracked before untracking.
- **T002 → T003**: untrack before adding the gitignore entry (the `.gitignore` line is only meaningful once the file is no longer tracked).
- **T002 + T003 → T012/T013**: the validation steps depend on both the untrack and the gitignore entry being in place.
- **T007–T010 → T011**: the doc-coverage grep check depends on all four doc edits being done.
- **T004–T006** are read-only confirmations with no dependency on the others; they can run any time.

## Parallel

- **T004, T005, T006** ([P]) — independent read-only confirmations across three different source files.
- **T007, T008, T009, T010** ([P]) — four independent documentation files, no shared edits.
- The confirmation set (T004–T006) and the documentation set (T007–T010) can also run concurrently with each other.
