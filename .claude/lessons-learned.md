# Lessons Learned — fed by `/fix-tickets`

Append-only, deduped lessons distilled from code-review + Copilot findings as the `/fix-tickets` loop runs.
**The fix and review subagents read this file before working**, so each ticket benefits from prior tickets' lessons — that's the compounding.

Rules for what goes here:
- Only **high-signal, reusable** lessons — a real bug class a reviewer caught, phrased as a do/don't. Not one-off style nits.
- Dedupe before appending. Tighten an existing line rather than adding a near-duplicate.
- Keep each lesson one or two lines, with the source ticket in parens.
- **Code conventions** guide the fix; **Loop operations** guide the skill; **Architecture / skill flags** are candidates to promote (to `CLAUDE.md`, an ADR, or a skill) — surfaced in the run report, not auto-applied.

---

## Code conventions (read before fixing)

- **Gate `/speckit.companion.*`-dependent UI on the extension dir** (`.specify/extensions/companion/`), not on preset presence — presets only swap the `speckit.*` command bodies and never register the namespaced command family. A preset-only project must read as "not installed." (#218)
- **Guard synthetic list entries against name collision.** When injecting a synthetic entry with a reserved name/value (workflow option, etc.), skip it and log a warning if a user-defined entry already uses that name — otherwise you get duplicate DOM `<option>`s and a last-write-wins `Map` clobber. (#218)
- **Persist only resolvable identifiers.** Don't write a synthetic UI-only name (e.g. `speckit-turbo`) into `.spec-context.json` where downstream code resolves it — persist the resolvable base (`speckit`) and carry the variant via a separate pin (`profile: turbo`). (#218)
- **Derive UI fields from the full discriminator, not field-presence.** When surfacing a value from an *optional* history/record field (e.g. an active task id on a spec row), gate it on the complete discriminator (step + finish/kind), not just `field != null` — an optional field may be written in other contexts and leak into the wrong rows. Keep a legacy `kind == null` fallback so older records still work. (#238)
- **Fast-path / simple-mode lands at `tasks` + `ready-to-implement`, NOT "at the implement step".** The fold sets `--step tasks --status ready-to-implement`; implement is the *next* user-triggered step. Keep docs/changelogs/command bodies worded to the fold — don't say a fast-tracked spec "lands at implement". (#237)
- **A follow-on manifest/map entry needs a distinct key.** Writing a second record keyed off the same id clobbers the first — on #208 a staged-images entry reused `tempFileSet.id` and overwrote the temp-set entry, nulling its path and leaking the original dir. Use a derived key (`<id>-staged-images`). Same family as the synthetic-name collision (#218). (#208)
- **Implement terminates at `implemented`, not `completed`.** `completed` is reserved for the user's final Mark-Completed action. When the pipeline finishes implement autonomously, the spec's terminal `status` is `implemented` (the `.spec-context.json` capture script intentionally refuses to write `completed` for exactly this reason). Don't force `completed`. (#208)
- **Never interpolate a workspace path into a shell string.** Don't build `cd "${workspaceRoot}"` for a terminal command — a path with `"`/`` ` ``/`$`/`\` breaks quoting or injects. Set the VS Code terminal's structured `cwd` via `createTerminal({ cwd })` instead. (#234)
- **Use `echo`, not a `#` shell comment, for terminal hints.** Interactive zsh (macOS default) has `INTERACTIVE_COMMENTS` off, so a leading `#` errors as a command. `echo "..."` is safe. (#234)
- **Guard `e.target` with `instanceof Element` before `.closest()`.** In click-delegation handlers (including injected webview scripts), `e.target` can be a non-Element `EventTarget` or null and throw. (#234)
- **Gate availability on the direct install check at every call-site.** A turbo fallback inferred from the *project-default* resolver missed the explicit-turbo-pick path (it still dispatched an unresolvable command). Check `isCompanionInstalled()` directly wherever the namespaced command could be dispatched. (#234)
- **Marketing/README images must not invent fake UI that misrepresents a concept.** A generated "install" image showed Turbo as an AI-model dropdown choice — but Turbo is a *template profile*, not a model. Prefer an honest feature list over fabricated UI. (#234)
- **Close the implement step from the always-on `tasks.md` watcher, not terminal/hook paths.** Implement has no "next step" to trigger its close; IDE-chat dispatch returns no terminal handle; stock mode has no hook — so terminal/hook close paths no-op for it. The `tasks.md` file watcher is the one mode-agnostic surface; write the terminal `implemented` close there (guarded by all-tasks-checked + underway + not-already-terminal). (#244)
- **`color-mix(..., transparent)` tokens are semi-transparent — document them by contrast ratio, not "effective hex".** A theme token derived via `color-mix(in srgb, var(--fg) 70%, transparent)` has no fixed color; its rendered value composites over whatever background it sits on. Don't write `~#bdbdbd`-style "resolves to" examples in comments/specs (they imply a guarantee that breaks across themes/backgrounds) — state the WCAG ratio target against the actual bg instead. (#254)
- **Thread a new navState/payload field through EVERY builder, not just the initial render.** The viewer replaces the whole `navState` object on each update (`buildViewerPayload` re-posts on every `.spec-context.json` change), so a field added only to the initial HTML navState shows on load then vanishes on the first refresh. Add it to `buildViewerPayload`'s rebuilt navState too. (#255)
- **When an authoritative signal drives a UI indicator, suppress secondary fallbacks while it's active.** The in-flight spinner keys off transition `status`; the old history fallback (`activeStep===step && !completedAt`) must fire ONLY when status gives no guidance (`draft`/unknown) — otherwise an in-flight status plus a stale `activeStep` can light two indicators at once. Gate the fallback on "status is neither in-flight nor settled." (#255)
- **JSX/TSX does not decode HTML entities in text nodes** — `&amp;` renders literally as "&amp;". Use a raw `&` (or `{'&'}`) in Preact/React text to match server-rendered copy. (#255)
- **Upsert by MERGING onto the existing record, never by rebuilding it.** A capture writer that did `summaries[id] = {status, did?, files?}` each call dropped hand-authored fields the reader supports (`concerns`) and erased prior `did`/`files` on a bare re-journal — breaking the non-destructive/backfill contract. Read the existing entry, copy it, then set only the fields you have a new non-empty value for. (Same clobber family as the synthetic-name / follow-on-key lessons.) (#256)
- **Green-baseline stale tests by re-deriving fixtures from the current contract — never weaken to pass — and gate the suite in CI.** When a test fails because the contract moved (e.g. #138 moved a per-task id from `substep` to a `task` field), update the assertion to the new shape while keeping it load-bearing (it must still fail on a real regression; verify by simulating one). If a "stale" test actually describes behavior the code should have but lacks, that's a real bug — leave it failing and file it, don't loosen it. An append-only test should assert prior entries are byte-for-byte unchanged, not just that the sequence looks right. Then wire the suite into CI (no `continue-on-error`/`|| true`) so a red baseline can't hide future regressions. (#263)
- **When you make a runtime value first-class, widen its TYPE too — don't cast it to `string`.** A new status was real at runtime (`SpecStatuses.IMPLEMENTED`) but the `SpecStatus` union still said `active|completed|archived`, forcing `as string` casts that silently disabled exhaustiveness checking everywhere status is switched. Add the value to the canonical union (mirror the runtime const) and delete the casts — the compiler then finds the next missing-case bug for you. (#257)
- **A menu `when` that newly matches a contextValue must have a handler that actually ACTS on it.** Adding `spec-implemented` to a tree row surfaced the Reactivate menu on implemented specs, but the command no-ops for non-completed/archived — a dead, misleading click. When you broaden a row's contextValue, re-check every `view/item/context` menu `when` it now matches and scope out the ones whose command would no-op. (#257)
- **`text-overflow: ellipsis` needs `white-space: nowrap` + `overflow: hidden` together — all three or it silently wraps.** A responsive fix added `text-overflow: ellipsis` + `min-width: 0` but no `nowrap`, so the text wrapped instead of truncating. Ship the full trio (and on a flex child, `min-width: 0` to let it shrink). (#258)
- **Preact `style` accepts a string (unlike React) — don't "fix" string-style props in webview/Storybook `.tsx`.** A reviewer flagged `style="..."` as a React error, but the webview is Preact, which renders `style.cssText` from a string, and the mock files use that convention throughout. Leave string styles in Preact components; only an *inconsistent* mix is worth touching. (#258)
- **A settings migration must rewrite ONLY known legacy values, and coerce defensively at read time.** Collapsing a tri-state enum (`off|beta|on`) to boolean: (1) migrate per-scope via `inspect()` + `update(..., sameTarget)` so a global vs workspace override isn't relocated; (2) rewrite only the known legacy strings — leave a typo/unknown string for VS Code to flag, don't blanket-coerce it; (3) make EVERY reader funnel through a coercion helper (`'off'→false`, `'beta'|'on'→true`, fallback to the per-setting default), because `Boolean("off") === true` would otherwise read a disabled feature as enabled before/around migration. (#259)
- **Swapping a convenience API endpoint for a list endpoint silently drops everything the convenience did for free.** Moving GitHub `…/releases/latest` → `…/releases` lost three protections at once: (1) `/latest` excludes drafts/prereleases — the list doesn't, so re-filter `draft`/`prerelease` yourself; (2) the list paginates (default 30) — with interleaved tag namespaces the target can fall off page 1, so set `?per_page=100` (or follow `Link` pages); (3) re-check incidental headers the old call relied on — GitHub REST `fetch()` with no `User-Agent` 403s. When you replace a "give me the one right thing" endpoint with "give me the list," you now own the selection AND the filtering it did implicitly. (#274)
- **Type external API responses to the API's real schema, not the fields you happen to read.** A `GitHubRelease` type marked `name`/`html_url` as required `string`, but REST returns `name: string | null` and the test stubs omit both. Mark genuinely-nullable fields `| null` and unconsumed/optional fields `?` — over-requiring `string` encourages unsafe non-null assumptions at call sites. (#274)
- **`A && B || C` is NOT if-then-else — C also runs when B fails.** A `gh release view X && gh release upload … || gh release create …` chain falls through to `create` on a transient `upload` failure, then errors on the already-existing tag and masks the real fault. For "do C only when the probe A fails," use an explicit `if A; then B; else C; fi`. (#273)
- **An invariant that must hold on EVERY run needs idempotent re-assertion, not set-once-at-create.** Setting `--prerelease` only on the create path left a reused release that lost the flag stuck as a normal release (which could become `/releases/latest`). Re-assert the invariant each run (`gh release edit … --prerelease` after the upsert), cheaply and idempotently. (#273)
- **Backticks inside a double-quoted shell string command-substitute — single-quote literal text.** A `gh release --notes "… \`companion\` …"` body risks executing `companion`/`specify …` when a user pastes the snippet. Use single quotes for notes/descriptions that contain backticks (no interpolation needed). (#273)
- **When you broaden a change's scope mid-PR, update the spec/PR-description constraint in the SAME commit.** Touching the root README to fix an install URL while the spec still said "root README MUST NOT be touched" is a self-contradiction reviewers flag immediately. If a forbidden touch becomes justified (a factual link-accuracy fix isn't a versioned-doc change), amend the constraint to permit exactly that and note it in the PR body. (#273)
- **Version literals scattered across a release doc drift on every bump — one source + placeholders.** A catalog-submission YAML and a pre-submit checklist both hardcoding the version went stale after `0.3.0→0.4.0`. Point checklist lines at the source of truth (`matches extension.yml`) or use `X.Y.Z`; only the actually-published artifact carries the literal. (#273)
- **An inline config default MUST match the `package.json` manifest default — or a read off the inline path silently drops it.** A resolver's hardcoded fallback `['specs']` diverged from the manifest default `['specs', '.specify/specs']`. Production works (VS Code merges the manifest default), but any bare/early config read off the inline fallback reintroduces the original bug (#270's welcome-screen-never-clears). When you change a setting's manifest default, grep for the same literal as an inline fallback and align it. (#277)
- **`escapeHtml` via textContent→innerHTML does NOT escape attribute quotes — never interpolate user data into an HTML attribute through `innerHTML`.** The webview's `escapeHtml` (`div.textContent = x; return div.innerHTML`) escapes `<`/`>`/`&` but leaves `"`/`'` intact, so a user-controlled value (a filename) inside `alt="${escapeHtml(name)}"` can break out of the attribute and inject. Build any markup containing user data with DOM APIs — `createElement` + `textContent` + `setAttribute` — which are injection-proof by construction. `escapeHtml` is only safe for element-*content* interpolation. (#272)
- **`hidden` / `display:none` removes an element from the accessibility tree — use a visually-hidden (`.sr-only`) class when `aria-describedby` points at it.** A char-counter hidden with the `hidden` property until 90% meant screen readers never heard the limit (the `aria-describedby` target wasn't in the a11y tree). Toggle an sr-only class (clip/position off-screen) instead so it stays discoverable while visually hidden. (#272)
- **`aria-busy` goes on the content region that becomes busy, not the transient overlay.** Set it on `<main>`/`#app` during submit, not the loading overlay element, so AT describes the form as busy. (#272)
- **Enum-by-TYPE is not enum-by-DATA — coerce at the privacy/telemetry boundary.** A field typed `'standard'|'turbo'` (or a workflow "phase", a setting value) can still carry arbitrary free text when it's sourced from user-authored data — `settings.json` accepts any string, `.spec-context.json` is hand-/hook-editable, custom workflows have user-named steps. Sending it verbatim leaks PII even though the TS type says it's safe. Allow-list every such value at the emit boundary (`x === 'a' || x === 'b' ? x : <default>`), don't trust the declared type. Bonus: fire any activation/settings snapshot AFTER the settings migration runs, and funnel migrated booleans through the same coercion helper the rest of the code uses. (#129)
- **Deleting a UI component drops every capability it owned — confirm the replacement covers the SAME cases, not a subset.** Removing `GeneratingFooter` deleted the "Mark step complete" manual override + recovery-timeout for ALL lifecycle steps, but the surviving backstop (the `tasks.md` watcher settle) only covers **implement** — leaving specify/plan/tasks with no manual un-stick. Before deleting a component, enumerate what it does and verify each capability is re-homed or genuinely obsolete; a "safe once X lands" assumption is only safe where X actually reaches. (#277, tracked → #281)
- **Trace a deleted component's FULL message/handler/state chain — deletion leaves orphans.** Removing `GeneratingFooter` left `handleMarkStepComplete` routing, the `markStepComplete` message type (both type files), write-only `ViewerState` fields, and a per-refresh disk probe with no consumer. When you delete a webview component, follow its `postMessage` → handler → state fields and remove (or re-home) the whole chain, not just the `.tsx`. (#277)
- **Guard a command/namespace family by its shared PREFIX, not an enumerated subset.** The missing-extension fallback detected "is this a companion command?" via an enumerated map of the 4 pipeline commands (`command in STOCK_COMMAND_BY_COMPANION`), so a 5th member of the family (`speckit.companion.mark-complete`) slipped past the guard and would dispatch an unresolvable command. Key off the `speckit.companion.` prefix; suppress (return null) members that have no stock twin rather than dispatching them. Enumerated allow/deny lists rot the moment the family grows. (#300)
- **Before making an activation-time step unconditional, confirm its inputs/paths always exist.** Removing a gate (`shouldEnsureStandard`) made `ensureStandardFamily()` run on every activation, but its bundled preset path lives inside the optional companion extension dir (`.specify/extensions/companion/presets/`) — so in projects without that extension it ran a failing `specify preset add` and logged errors on every activation, not rerunning until a reload. Gate the ensure on the on-disk install signal (`isCompanionInstalled`) and rerun it from the existing extension-dir file watcher — don't run-and-fail-quietly each activation. (#300)

## Loop operations (guide `/fix-tickets` itself)

- **install-local is step 1 of every ticket** — install the previous ticket's merge before fixing, so each fix runs on the freshest build + turbo commands.
- **Restore `.specify/` along with `package.json`/`package-lock.json`** after install-local — `specify extension add` regenerates registry artifacts (`extensions.yml`, `.registry`, `feature.json`) that otherwise leave the tree dirty and trip the clean-tree guard.
- **Request Copilot via REST** `requested_reviewers` with login `copilot-pull-request-reviewer[bot]`. `gh pr edit --add-reviewer Copilot` fails ("Could not resolve user with login 'copilot'").
- **Conditional 2nd Copilot pass: re-request after a fix that changed real LOGIC, skip for docs/CSS/label fixes.** The fix you push to address Copilot's first round is itself unreviewed (not by Copilot, not by local review) — and on logic fixes that's the highest-risk commit. Re-request Copilot and poll for NEW inline comments (count > the baseline you captured before pushing); if none in ~12 min, that's the all-clear, merge. This caught real logic bugs on #255/#256/#257/#259's first round; the 2nd pass on #259 came back clean. Don't double-loop trivial doc/CSS rounds — it just adds ~5–12 min for no signal.
- **Copilot is fast** (~4–5 min) and earns its place — #218: caught two real correctness issues; #238: caught an optional-field leak; #237: caught doc wording the *local review itself had just written* ("lands at implement"). The review→Copilot two-layer pass keeps catching each other's misses — keep both. Poll at ~90s, not 30s (gentler, Copilot takes ~4 min anyway).
- **Dogfood `.spec-context.json` is a byproduct, not the feature.** A turbo-pipeline run records its own history into `specs/<NNN>/.spec-context.json`; it may carry backfilled/odd-ordered implement entries. Don't reorder it in-PR (that means fabricating timestamps/authorship) — the real implement-lifecycle ordering is tracked in #229/#244. But DO fix non-fabricated defects: a `[FEATURE NAME]` placeholder specName, or a wrong terminal `status` (see implement→`implemented` above), are corrections, not fabrication.
- **Wait ~5 min before the first Copilot poll.** Copilot reviews take ~4–5 min, so polling immediately just burns no-op checks. `sleep 300` first, then poll at 90s.
- **Fix subagents must not commit `.specify/` artifacts.** `git add -A` sweeps `.specify/feature.json` (the spec-session pointer regenerated by the pipeline, + registry files) into the feature commit. Commit only `src/`/`webview/`/`package.json` + the `specs/<NNN>/` folder; `git checkout origin/main -- .specify/<file>` for any `.specify/*` that shows modified. (#238)
- **Verify a queued ticket still reproduces on current `main` before building — backlog goes stale.** This session 3 of 8 queued items were already (partly) fixed: the reconciler catalog-seam, #241 (a dup of merged #243), and #229's state-clearing (only its icon ask was live). Have the fix subagent confirm the bug first and deliver ONLY the genuinely-missing part (close the dup, or build just the gap) — don't rebuild what's there.
- **Commit the code-review subagent's `--fix` edits.** A review subagent applied a real CSS fix to the working tree but returned without committing it (#229); the orchestrator must check `git status` after review and commit any left-behind fix before opening the PR.
- **When the internal review flags a latent landmine in newly-added code, fix it in the SAME PR — don't defer it.** Both #300 findings (prefix-vs-subset guard, unconditional activation step) were exactly what the in-repo code-review subagent had flagged as "latent / out of scope," but Copilot escalated them to actionable. The next reviewer will (rightly) call out a landmine you introduced in this diff. If the review subagent marks something latent in code this PR is adding/touching, treat it as in-scope and fix it now rather than passing it to Copilot or a follow-up. (#300)
- **Have the fix subagent challenge the ticket's proposed MECHANISM, not just whether the bug exists.** A ticket can be real but its suggested fix architecturally wrong: #273 proposed `…/releases/latest/download/companion.zip`, which 404s in this two-product repo the moment a VS Code `v*` release is cut. The subagent verified the premise, deviated to a robust alternative (a dedicated `companion-latest` prerelease tag), and flagged the deviation. Instruct fix subagents to validate the approach against the codebase's reality and ship the sound version, reporting any deviation. (#273)

## Architecture / skill flags (candidates to promote — not auto-applied)

- **The two-product-one-releases-list model is a recurring footgun — document it once, centrally.** Both #274 (update checker compared against the wrong product's release) and #273 (install URL would 404 across product interleaving) were caused by `v*` (VS Code) and `speckit-ext-v*` (spec-kit ext) sharing one GitHub releases list, with `/releases/latest` resolving across both. The `companion-latest` prerelease tag (stable spec-kit-ext download) and the `^v\d+\.\d+\.\d+$` filter (update checker) are the two guards. Candidate: a short "Release tag model" section in CLAUDE.md or `docs/` so the next release-touching change doesn't rediscover this. (#273, #274)

- **turbo `specify` can leave the `[FEATURE NAME]` placeholder in `.spec-context.json` `specName`.** Seen on #208 — the seed metadata wasn't filled with the real spec name. Worth a fix in the specify command body / capture seed so the name is always populated. (#208)
- **Spec-editor webview paths lack unit coverage.** `getWorkflows()` reads live `vscode.workspace` config, so the collision/gating logic is review-only today. A vscode-config mock harness would let those branches get a regression guard. (#218)
