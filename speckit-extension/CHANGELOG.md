# Changelog — moved

The spec-kit extension's changelog now lives at
<https://github.com/alfredoperez/speckit-companion/blob/main/apps/speckit-extension/CHANGELOG.md>.

This file is a redirect stub, kept so the link pinned by the Spec Kit community
catalog resolves instead of 404ing. It is never updated; every release, including
the current one, is recorded at the link above.
