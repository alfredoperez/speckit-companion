## Purpose

The living spec the delta fixtures modify. Registered as `spec-shape-target`.

## Requirements

### The thing behaves

It does the thing.

#### Scenario: the ordinary case
- **WHEN** asked
- **THEN** it does it
