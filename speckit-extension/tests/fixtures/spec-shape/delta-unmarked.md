# Feature Specification: A feature whose delta block carries no capability marker

An unmarked block belongs to whichever capability the changed files matched, so
it is checked against that spec exactly like a marked one.

## MODIFIED Requirements

### A rule the target does not have

Rewritten prose for a heading that is not in the target spec.

#### Scenario: the ordinary case
- **WHEN** asked
- **THEN** it does it
