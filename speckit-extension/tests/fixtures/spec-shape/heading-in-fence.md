## Purpose

A requirement heading inside a fenced block is an example, not a requirement.

## Requirements

### The only real requirement

It MUST do the thing, and the block below is documentation.

```markdown
### Not a requirement

This one has no scenario, and that must not be reported.
```

#### Scenario: the ordinary case
- **WHEN** asked
- **THEN** it does it
