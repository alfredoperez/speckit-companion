## Purpose

A marker is recognised only on the line immediately after its heading.

## Requirements

### Has a late marker

Some prose first.

<!-- touches: src/late/** -->

The comment above is body, not a marker.
