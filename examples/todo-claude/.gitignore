node_modules/
dist/
bench/.run-state.json
bench/.last-vitest.json
# bench-managed: flips per run / --dev install artifacts
.specify/companion.yml
.specify/presets/
# speckit-extension install artifacts (regenerated by `specify extension add`)
.specify/extensions/
.specify/extensions.yml
