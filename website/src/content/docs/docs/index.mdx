---
title: Placeholder overview
description: Scaffold page. No documentation has been written yet.
---

import DocsEmptyState from '../../../components/docs/DocsEmptyState.astro';

<DocsEmptyState heading="Nothing is written here yet">
  This page is a placeholder created by the scaffolding ticket. Nothing here describes the product yet.
</DocsEmptyState>

The real overview gets written in a later ticket, from a claim ledger where every sentence is traced to a file and symbol in the extension source. Until that ledger exists, this page stays empty on purpose rather than guessing.

## What this page will hold

- What the extension is, in one paragraph
- The pipeline steps, in the order they run
- Links into the deeper pages

## Route check

This page lives at `/docs/`. The landing page at `/` is a plain Astro route and does not use the Starlight layout.
