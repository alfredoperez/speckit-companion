---
title: Placeholder install page
description: Scaffold page. Install instructions have not been written yet.
---

This page is a placeholder created by the scaffolding ticket. It carries no install instructions yet.

There are two separately versioned pieces in this repo and they install by different routes, so the real page has to cover both without blurring them together. Writing that from memory is how wrong instructions ship, so it waits for the ticket that reads the release flow first.

## What this page will hold

- The VS Code extension install path
- The spec-kit extension install path, which is not the same one
- What to check when an install did not take
