.vscode/**
.vscode-test/**
.venv/**
.uv-cache/**
__pycache__/**
**/__pycache__/**
*.pyc
apps/vscode/src/**
# Visual-regression baselines. `**/*.ts` drops the webview sources but not the
# PNGs beside them, so 100 screenshots — the whole approved set, both widths and
# both themes — were riding along in every .vsix. Nothing at runtime reads them.
**/__screenshots__/**
.gitignore
.yarnrc
vsc-extension-quickstart.md
**/tsconfig.json
**/.eslintrc.json
**/*.map
**/*.ts
**/.git/**
.github/**
**/temp/**
temp/**
node_modules/**
README*.md
.DS_Store
*.vsix
!assets/icons/**
!assets/sample-spec/**
# Media for contributes.walkthroughs. VS Code resolves these paths against the
# installed extension root, so the Get Started page breaks if they miss the .vsix.
!assets/walkthrough/**
.claude/**
CLAUDE.md
tooling/scripts/**
apps/vscode/tests/**
*.test.ts
*.test.js
jest.config.js
tsconfig.test.json
coverage/**
dist/__tests__/**
docs/**
webpack.config.js
*.log
**/*.ts
apps/vscode/src/**/*.js

# Dev workspace — never runtime code (vsce ignores .gitignore when this file exists,
# so gitignored caches/build output must be listed here too)
.codex/**
specs/**
bench/**
# The marketing/docs site. Separate package.json, separate lockfile, separate
# deploy. Its node_modules and dist would otherwise land in the .vsix, since
# vsce ignores .gitignore whenever this file exists.
apps/website/**
apps/speckit-extension/**
# Stub left behind at the pre-move path — never ships in the .vsix.
speckit-extension/**
# Ship the context writer inside the .vsix: the stock-mode prompt preamble
# references it by the extension's install path (see promptBuilder).
# Its sibling modules ship too — the writer imports them at startup.
!apps/speckit-extension/scripts/write-context.py
!apps/speckit-extension/scripts/spec_context.py
!apps/speckit-extension/scripts/spec_deltas.py
!apps/speckit-extension/scripts/capture.py
!apps/speckit-extension/scripts/task_sync.py
!apps/speckit-extension/scripts/resolve-spec-paths.py
!apps/speckit-extension/scripts/living_spec_fold.py
!apps/speckit-extension/scripts/living_validate.py
!apps/speckit-extension/scripts/run_trace.py
# The shared atomic-write helper. The capture writers publish through it; each
# guards the import and falls back to its old fixed-`.tmp` write, so leaving it
# out would silently downgrade the crash-safety guarantee rather than fail loudly
# — which is exactly the kind of quiet regression it exists to prevent.
!apps/speckit-extension/scripts/companion_config.py
# The pipeline builder runs these against the extension's own node sources, so a
# project consuming the extension can build and draw its pipeline without a
# separate install. Leaving them out ships a panel that can only say it is
# unavailable — the "contributed but unreachable" failure this repo keeps fixing.
!apps/speckit-extension/scripts/build-pipeline.py
!apps/speckit-extension/scripts/pipeline-graph.py
!apps/speckit-extension/scripts/config_write.py
!apps/speckit-extension/scripts/config_repair.py
!apps/speckit-extension/scripts/hook_render.py
!apps/speckit-extension/scripts/template_render.py
!apps/speckit-extension/scripts/decision_routes.py
!apps/speckit-extension/scripts/manifest.py
!apps/speckit-extension/scripts/assemble_nodes.py
!apps/speckit-extension/scripts/instruction-budget.py
!apps/speckit-extension/scripts/_command_parts.py
!apps/speckit-extension/scripts/emission_sync.py
# …and the sources they assemble from.
!apps/speckit-extension/nodes/**
!apps/speckit-extension/presets/_parts/**
!apps/speckit-extension/fragments/**
!apps/speckit-extension/workflows/presets/**
.specify/**
.opencode/**
.serena/**
.cursor/**
.agents/**
.gemini/**
.qwen/**
.windsurf/**
.storybook/**
content/design/**
storybook-static/**
**/*.stories.tsx
assets/mascot/**
# Clip compositions, renders and site cards. Docs/README sources only; nothing at runtime reads them.
content/media/**
assets/social/**
AGENTS.md
package-lock.json
# The spec-kit half's manifest: the extension compares its bundled version against the
# workspace install to say when the command half is behind, with no network call.
!apps/speckit-extension/extension.yml
